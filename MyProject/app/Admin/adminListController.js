(function (app) {

    app.controller('adminListController', ['$scope',
        function ($scope, loginService, $injector, notificationService) {
        // do something
        }]);
})
(angular.module('MyApp.Admin'), []);